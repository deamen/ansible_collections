---
- name: Test deamen.ai.install_gemini_cli
  hosts: molecule:&containers
  gather_facts: true

  tasks:
    - name: Install Gemini CLI
      ansible.builtin.import_role:
        name: deamen.ai.install_gemini_cli

    - name: Install Pi code
      ansible.builtin.import_role:
        name: deamen.ai.install_pi_code
